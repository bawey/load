@Watched(label='Ngine Time')
select current_timestamp() from pattern[every timer:interval(333 msec)];

@Watched(label='Ngine Date')
select date(current_timestamp()) from pattern[every timer:interval(333 msec)];